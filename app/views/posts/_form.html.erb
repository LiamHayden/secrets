<%= form_for Post.new, data: { turbo: false } do |form| %>
	<%= form.label "Title:" %>
	<%= form.text_field :title %>

	<%= form.label "Body:" %>
	<%= form.text_field :body %>

	<%= form.submit "Submit" %>
<% end %>
